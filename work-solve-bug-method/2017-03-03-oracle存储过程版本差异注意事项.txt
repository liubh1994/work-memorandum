Oracle10g中:
在存储过程里直接select max(t.serialno) into vSerialno from tstudent; 
在存储过程里打开一个游标,然后循环处理游标数据,当用游标数据对变量赋值时;
在存储过程里打开游标,然后循环处理游标数据,当对游标数据使用聚合函数时；
--------------------------------------以上都会报错:字符串缓冲区太小------------------------------
解决方法:换种方式写或者使用oracle 11g